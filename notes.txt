-> use npm install --legacy-peer-deps to install all the pending dependencies
-> axios -> to make api calls
-> material ui -> for ui related stuff
-> router-dom -> for routing related stuff

-> path= '/video/:id' -> every video will have its specific id to route to 